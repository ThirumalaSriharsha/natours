extends baseEmail

block content 
    p Hi #{firstName},
    p forgot your password ? submi a patch request with ypur new password amd password conformation
    to :#{url}
        table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0')
            tbody
                tr
                    td(align='left')
                        table(role='presentation' border='0' cellpadding='0' cellspacing='0')
                            tbody
                                tr
                                    td
                                        a(href=`${url}` target='_blank') update your password
    p if you dont forgot your password please ignore the maill!
